
mod quantum {
    // for now just storing real values amplitudes.
    struct Qubit {
        alpha: f64,
        betha: f64
    }

    
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}


